{
  "name": "agent-memory-mcp",
  "version": "0.1.0",
  "description": "Obsidian-backed persistent memory layer for AI agents (MCP server over stdio)",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "reindex": "tsx src/cli.ts reindex",
    "stats": "tsx src/cli.ts stats",
    "doctor": "tsx src/cli.ts doctor",
    "search": "tsx src/cli.ts search"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "yaml": "^2.8.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}